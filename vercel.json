{
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api/index" },
    { "source": "/integrations/(.*)", "destination": "/api/index" }
  ],
  "installCommand": "npm install --legacy-peer-deps"
}
